{% extends '::lyt_inicio.html.twig' %}
{% block form %}
{% if error %}
    <div>{{ error.message }}</div>
{% endif %}
	<form action="{{ path('login_check') }}" method="post">
	    <label for="username">Username:</label>
	    <input type="text" id="username" name="_username" value="{{ last_username }}" requeired />

	    <label for="password">Password:</label>
	    <input type="password" id="password" name="_password" />

	    <button type="submit"  class="btn btn-primary btn-block login" >login</button>

	   	<a href="{{ path('usuario_new') }}" class="btn btn-success btn-block login" type="button">Registrarse</a>

	</form>

{% endblock %}
