{% extends '::base.html.twig' %}

{% block body -%}
    <h1>NivelEstudio</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nivel</th>
                <td>{{ entity.nivel }}</td>
            </tr>
            <tr>
                <th>Estnivel</th>
                <td>{{ entity.estNivel }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('nivelestudio') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('nivelestudio_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
