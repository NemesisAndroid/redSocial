{% extends '::base.html.twig' %}

{% block body -%}
    <h1>GeneroExtra list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Genero</th>
                <th>Estgenero</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('generoextra_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.genero }}</td>
                <td>{{ entity.estGenero }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('generoextra_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('generoextra_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('generoextra_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
