{% extends '::base.html.twig' %}

{% block body -%}
    <h1>GeneroExtra</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Genero</th>
                <td>{{ entity.genero }}</td>
            </tr>
            <tr>
                <th>Estgenero</th>
                <td>{{ entity.estGenero }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('generoextra') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('generoextra_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
